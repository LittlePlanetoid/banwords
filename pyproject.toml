[project]
name = "banwords"
version = "0.1.0"
description = "Python tool for scanning projects"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "argparse>=1.4.0",
    "logging>=0.4.9.6",
]
